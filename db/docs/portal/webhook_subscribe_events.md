# webhook_subscribe_events

## Description

<details>
<summary><strong>Table Definition</strong></summary>

```sql
CREATE TABLE `webhook_subscribe_events` (
  `webhook_id` char(36) NOT NULL,
  `event_type` varchar(64) NOT NULL COMMENT 'Event type: user.created, group.member_added, etc.',
  `created_at` datetime(6) NOT NULL DEFAULT current_timestamp(6),
  PRIMARY KEY (`webhook_id`,`event_type`),
  CONSTRAINT `fk_webhook_events_webhook` FOREIGN KEY (`webhook_id`) REFERENCES `webhooks` (`id`) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
```

</details>

## Columns

| Name | Type | Default | Nullable | Children | Parents | Comment |
| ---- | ---- | ------- | -------- | -------- | ------- | ------- |
| webhook_id | char(36) |  | false |  | [webhooks](webhooks.md) |  |
| event_type | varchar(64) |  | false |  |  | Event type: user.created, group.member_added, etc. |
| created_at | datetime(6) | current_timestamp(6) | false |  |  |  |

## Constraints

| Name | Type | Definition |
| ---- | ---- | ---------- |
| fk_webhook_events_webhook | FOREIGN KEY | FOREIGN KEY (webhook_id) REFERENCES webhooks (id) |
| PRIMARY | PRIMARY KEY | PRIMARY KEY (webhook_id, event_type) |

## Indexes

| Name | Definition |
| ---- | ---------- |
| PRIMARY | PRIMARY KEY (webhook_id, event_type) USING BTREE |

## Relations

![er](webhook_subscribe_events.svg)

---

> Generated by [tbls](https://github.com/k1LoW/tbls)
