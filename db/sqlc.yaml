version: "2"
sql:
  - engine: "postgresql"
    queries: query/oidc.sql
    schema: schema.sql
    strict_function_checks: true
    gen:
      go:
        package: "gen"
        out: "../pkg/infrastructure/postgres/gen"
        sql_package: "pgx/v5"
  - engine: "postgresql"
    queries: query/portal.sql
    schema: portal-schema.sql
    strict_function_checks: true
    gen:
      go:
        package: "gen"
        out: "../pkg/infrastructure/portal/gen"
        sql_package: "pgx/v5"
