host: http://localhost:8080
environment: development

database:
  host: localhost
  port: 5433
  user: root
  password: password
  name: oidc

portal:
  database:
    host: localhost
    port: 5432
    user: root
    password: password
    name: portal

oauth:
  test_user_id: testuser
