# user_statuses

## Description

<details>
<summary><strong>Table Definition</strong></summary>

```sql
CREATE TABLE `user_statuses` (
  `user_id` char(36) NOT NULL,
  `status` varchar(64) NOT NULL COMMENT 'Status type: active, suspended, email-unconfirmed, etc.',
  `detail` varchar(255) DEFAULT NULL COMMENT 'Additional detail/reason',
  `created_at` datetime(6) NOT NULL DEFAULT current_timestamp(6),
  PRIMARY KEY (`user_id`,`status`),
  KEY `idx_user_statuses_status` (`status`),
  CONSTRAINT `fk_user_statuses_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
```

</details>

## Columns

| Name | Type | Default | Nullable | Children | Parents | Comment |
| ---- | ---- | ------- | -------- | -------- | ------- | ------- |
| user_id | char(36) |  | false |  | [users](users.md) |  |
| status | varchar(64) |  | false |  |  | Status type: active, suspended, email-unconfirmed, etc. |
| detail | varchar(255) | NULL | true |  |  | Additional detail/reason |
| created_at | datetime(6) | current_timestamp(6) | false |  |  |  |

## Constraints

| Name | Type | Definition |
| ---- | ---- | ---------- |
| fk_user_statuses_user | FOREIGN KEY | FOREIGN KEY (user_id) REFERENCES users (id) |
| PRIMARY | PRIMARY KEY | PRIMARY KEY (user_id, status) |

## Indexes

| Name | Definition |
| ---- | ---------- |
| idx_user_statuses_status | KEY idx_user_statuses_status (status) USING BTREE |
| PRIMARY | PRIMARY KEY (user_id, status) USING BTREE |

## Relations

![er](user_statuses.svg)

---

> Generated by [tbls](https://github.com/k1LoW/tbls)
